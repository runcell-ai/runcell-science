# Open Science Web

React + Vite + TypeScript frontend shell for Open Science.

This package currently contains only the non-business app frame:

- Session list panel placeholder
- Chat panel placeholder
- Artifacts / extensions panel placeholder
- shadcn/ui component setup

Run from the repository root:

```bash
yarn dev:web
```

The root `README.md` contains the full workspace setup and verification commands.
